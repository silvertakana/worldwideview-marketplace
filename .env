# Default local database URL for Prisma
DATABASE_URL="file:./prisma/registry.db"

# Satisfies the worktree-env-guard hook (marketplace uses Supabase, not NextAuth, so this value is unused at runtime)
AUTH_SECRET=marketplace-local-placeholder
